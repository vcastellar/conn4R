% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clases.R
\docType{class}
\name{arbol-class}
\alias{arbol-class}
\title{Clase S4 para representar el árbol de búsqueda minimax}
\description{
La clase \code{arbol} almacena todos los nodos evaluados durante
  una llamada al algoritmo minimax con \code{guardar_arbol = TRUE}. Cada
  nodo ocupa una posición en los vectores de slots; el índice común es
  \code{idNodo}.

  Se puede recorrer el árbol de forma top-down con
  \code{\link{encontrar_mejor_variante}} para extraer la línea de juego
  óptima, y visualizarla paso a paso con
  \code{\link{mostrar_mejor_variante}}.
}
\section{Slots}{

\describe{
\item{\code{idNodo}}{Vector entero. Identificador único del nodo (1, 2, 3, …).}

\item{\code{idPadre}}{Vector entero. \code{idNodo} del nodo padre;
\code{NA} para la raíz.}

\item{\code{jugada}}{Vector entero. Columna (1-7) jugada para alcanzar el nodo;
\code{NA} para la raíz.}

\item{\code{turno}}{Vector lógico. \code{TRUE} si en este nodo mueve la IA
(nodo MAX); \code{FALSE} si mueve el humano (nodo MIN).}

\item{\code{profundidad}}{Vector entero. Nivel del nodo en el árbol (0 = raíz).}

\item{\code{puntuacion}}{Vector numérico. Evaluación minimax del nodo, asignada
tras explorar el subárbol correspondiente.}
}}

\examples{
\dontrun{
tablero <- crear_posicion_aleatoria(7)
res <- minimax(tablero, profundidad = 4, maximizandoIA = TRUE,
               guardar_arbol = TRUE)
arbol <- res$arbol
length(arbol@idNodo)          # número total de nodos explorados
head(data.frame(
  nodo  = arbol@idNodo,
  padre = arbol@idPadre,
  col   = arbol@jugada,
  punt  = arbol@puntuacion
))
}

}
\seealso{
\code{\link{minimax}}, \code{\link{encontrar_mejor_variante}},
  \code{\link{mostrar_mejor_variante}}
}
