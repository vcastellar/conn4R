% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clases.R
\name{mostrar_mejor_variante}
\alias{mostrar_mejor_variante}
\title{Reproducir visualmente la mejor variante de una partida}
\usage{
mostrar_mejor_variante(tablero_inicial, variante, pausa_ms = 1000)
}
\arguments{
\item{tablero_inicial}{Matriz 6×7 con el estado del tablero en el momento
en que se inició el análisis minimax.}

\item{variante}{Lista devuelta por \code{\link{encontrar_mejor_variante}}.}

\item{pausa_ms}{Numérico. Milisegundos de espera entre cada jugada mostrada.
Por defecto \code{1000}.}
}
\value{
Invisible. La función se llama por su efecto secundario (visualización).
}
\description{
Toma la variante principal extraída por
  \code{\link{encontrar_mejor_variante}} y reproduce las jugadas una a una
  sobre el tablero inicial, imprimiendo el gráfico del tablero en cada paso
  con una pausa configurable entre movimientos.
}
\examples{
\dontrun{
tablero <- crear_posicion_aleatoria(7)
res <- minimax(tablero, profundidad = 4, maximizandoIA = TRUE,
               guardar_arbol = TRUE)
variante <- encontrar_mejor_variante(res$arbol)
mostrar_mejor_variante(tablero, variante, pausa_ms = 800)
}

}
\seealso{
\code{\link{encontrar_mejor_variante}}, \code{\link{visualizar_tablero}}
}
